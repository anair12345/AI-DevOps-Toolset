name: devops-ai-portal
image:
  repository: devops-ai-portal
  tag: local
  pullPolicy: IfNotPresent
replicaCount: 1
service:
  type: ClusterIP
  port: 80
containerPort: 8501
env:
  - name: TOOLKIT_BASE_URL
    value: "http://alert-summariser"  # change to gateway/ingress if you add one
